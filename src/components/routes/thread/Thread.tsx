import React, { FC } from 'react'
import { IThread } from '../../../interfaces/IThread'
import "./Thread.css";
export const Thread: FC<IThread> = (props) => {
    return (
        <div>

        </div>
    )
}
